Vue.component("diary-entry",{props:["diary"],template:'<div class="card mb-5"><div class="card-header"><span class="text-secondary">{{ moment(diary.publish_date).format("lll") }}</span> by <strong>{{ diary.author }}</strong> <span class="badge badge-primary" v-if="!diary.public">Secret</span></div><div class="card-body"><h2 class="card-title">{{ diary.title }}</h2><p class="card-text">{{ diary.text }}</p></div></div>'});